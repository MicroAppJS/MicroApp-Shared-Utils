'use strict';

/* global expect */

// const prompt = require('.');

describe('prompt', () => {

    it('confirm', async () => {

        // const result = await prompt.confirm('are you sure?');

        // console.log(result);
    });

});
